[package]
name = "tmtcopen"
version = "0.1.0"
edition = "2024"

[dependencies]
gtk = { version = "0.18", features = ["v3_24"] }
gdk = "0.18"
cairo-rs = { version = "0.18", features = ["png"] }
glib = "0.18"
gdk-pixbuf = "0.18"
serde = { version = "1.0", features = ["derive"] }
confy = "0.6"